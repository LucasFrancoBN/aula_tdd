server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 12345
    url: jdbc:postgresql://localhost:5432/aula-tdd
  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    root: error
    org.springframework: error